# java-lotto-precourse
## 🚀 **미션 개요**

**로또 발매기 프로그램**을 구현한다.

사용자가 입력한 금액만큼 로또를 구매하고,

당첨 번호 및 보너스 번호를 입력받아 **당첨 결과와 수익률을 계산**한다.

이번 미션의 목표는 **객체 지향 설계와 단위 테스트**를 통해 안정적이고 유지보수 가능한 코드를 작성하는 것이다.

---

## 🗂️ 패키지 구조

```
java-lotto
├── Application.java                     
│
├── controller                          
│   └── LottoController.java             # 입출력과 서비스 로직 연결
│
├── service                              # 핵심 비즈니스 처리 계층
│   ├── LottoService.java                # 전체 로또 흐름 관리
│   └── LottoCalculator.java             # 당첨 결과 및 수익률 계산 로직
│
├── domain                               # 핵심 도메인 로직 (불변 규칙)
│   ├── Lotto.java                       # 로또 한 장을 나타내는 클래스
│   ├── LottoMachine.java                # 로또 발행기
│   ├── WinningLotto.java                # 당첨 번호 및 보너스 번호 관리
│   ├── MatchResult.java                 # 로또와 당첨 번호를 비교한 결과 보관
│   └──  Rank.java                        # 등수 및 상금 Enum
│  
├── dto                                
│   └── LottoResult.java                 # 당첨 통계 및 수익률 결과
│
├── util                                
│   ├── InputParser.java                 # 문자열 입력 파싱 및 변환
│   ├── LottoValidator.java              # 로또 번호 유효성 검증
│   └── LottoConstants.java              # 상수 관리 (가격, 범위 등)
│  
└── view                                 # 입출력 담당
    ├── InputView.java                   # 사용자 입력 처리
    └── OutputView.java                  # 결과 및 메시지 출력

test
├── java
│   └── lotto
│       ├── domain
│       │   ├── LottoMachineTest.java   # 로또 발행 및 금액 계산 테스트
│       │   ├── LottoResultTest.java    # 수익률 계산 테스트
│       │   ├── LottoTest.java          # 로또 번호 유효성 및 정렬 테스트
│       │   ├── RankTest.java           # 등수 및 상금 Enum 테스트
│       │   └── WinningLottoTest.java   # 당첨 번호 비교 및 보너스 검증 테스트
│       │
│       └── util
│           ├── InputParserTest.java    # 입력값 파싱 로직 테스트
│           └── LottoValidatorTest.java # 로또 번호 검증 로직 테스트
│
└── ApplicationTest.java               

```

---

## 🧩 기능 요구사항

### 1. 로또 구매 및 발행 기능

- [x]  구입 금액을 입력받는다.
    - [x]  금액이 1,000원 단위가 아니면 예외 발생
        
        → `[ERROR] 구입 금액은 1,000원 단위여야 합니다.`
        
    - [x]  금액이 숫자가 아닐 경우 예외 발생
        
        → `[ERROR] 잘못된 입력입니다. 숫자를 입력해 주세요.`
        
- [x]  구입 금액에 따라 구매 가능한 로또 개수를 계산한다.
    
    (예: 8,000원 입력 시 8개 발행)
    
- [x]  랜덤으로 **중복되지 않은 6개의 숫자(1~45)** 를 뽑는다.
    - [x]  중복 숫자가 존재할 경우 예외 발생
        
        → `[ERROR] 로또 번호는 중복될 수 없습니다.`
        
- [x]  생성된 로또 번호를 오름차순으로 정렬하여 출력한다.

### 2. 당첨 번호 입력 및 검증 기능

- [x]  당첨 번호 6개를 입력받는다. (쉼표 구분)
    - [x]  숫자가 아닌 값이 포함된 경우 예외 발생
        
        → `[ERROR] 숫자만 입력할 수 있습니다.`
        
    - [x]  중복된 번호가 있을 경우 예외 발생
        
        → `[ERROR] 당첨 번호는 중복될 수 없습니다.`
        
    - [x]  1~45 범위를 벗어나는 경우 예외 발생
        
        → `[ERROR] 로또 번호는 1부터 45 사이의 숫자여야 합니다.`
        
    - [x]  6개의 번호가 아닌 경우 예외 발생
        
        → `[ERROR] 당첨 번호는 6개여야 합니다.`
        
- [x]  보너스 번호를 입력받는다.
    - [x]  숫자가 아닌 경우 예외 발생
    - [x]  1~45 범위 밖의 숫자면 예외 발생
    - [x]  당첨 번호와 중복될 경우 예외 발생
        
        → `[ERROR] 보너스 번호는 당첨 번호와 중복될 수 없습니다.`
        

### 3. 당첨 결과 계산 기능

- [x]  사용자가 구매한 모든 로또를 순회하며 당첨 번호와 비교한다.
- [x]  일치한 숫자 개수와 보너스 번호 일치 여부를 판단한다.
- [x]  일치 결과에 따라 `Rank`(Enum)으로 등수를 구분한다.
    - [x]  6개 일치 → 1등 (2,000,000,000원)
    - [x]  5개 + 보너스 번호 일치 → 2등 (30,000,000원)
    - [x]  5개 일치 → 3등 (1,500,000원)
    - [x]  4개 일치 → 4등 (50,000원)
    - [x]  3개 일치 → 5등 (5,000원)
    - [x]  그 외는 미당첨
- [x]  당첨 내역을 출력 형식에 맞게 출력한다.

### 4. 수익률 계산 기능

- [x]  모든 당첨 금액의 총합을 계산한다.
- [x]  수익률을 구입 금액 대비 퍼센트(%)로 계산한다.
    - [x]  소수점 둘째 자리에서 반올림한다.
- [x]  “총 수익률은 X%입니다.” 형식으로 출력한다.

### 5. 예외 처리 및 프로그램 흐름 제어

- [x]  예외 발생 시 `IllegalArgumentException`으로 처리하고,
    
    에러 메시지를 `[ERROR]`로 시작하도록 출력한다.
    
- [x]  예외 발생 시 해당 입력 단계부터 다시 입력받는다.
- [x]  프로그램 종료 시 `System.exit()`를 호출하지 않는다.

### 6. 구조 및 테스트 관련

- [x]  **Enum**을 활용하여 등수와 상금 관리
- [x]  **상수(static final)** 로 하드코딩 방지
- [x]  **단위 테스트 작성**
    - [x]  `Lotto` 생성 유효성 검증 테스트
    - [x]  `Rank` 계산 테스트
    - [x]  `LottoResult` 수익률 계산 테스트
    - [x]  `WinningLotto` 비교 로직 테스트
- [x]  UI(`System.out`, `Console`) 관련 테스트는 제외한다.

---

### 코딩 컨벤션 및 리팩토링 원칙

- **`Lotto`** 클래스 외부에서 **`numbers`**를 직접 변경할 수 없도록 불변성 유지
- 입력값 검증 로직은 별도 유틸 또는 Validator로 분리
- 메서드는 15라인 이하로 작성
- 들여쓰기는 2단계 이내로 제한
- else, switch문 사용 금지 (조기 반환 return 사용)
- 필요 시 List, Map 등을 활용하여 결과를 효율적으로 집계

---

### 💡 초기 계획 변경사항 및 리팩토링

초기에는 대부분의 비즈니스 로직이 `LottoController`와 `LottoResult` 내부에 집중되어 있다고 느껴, 각 계층의 역할을 명확하게 분리해보고 싶었다.

1. Controller의 책임 분리

**변경 전**

- `LottoController`가 입력 → 로또 발행 → 결과 계산 → 출력까지 모든 과정을 직접 수행함
- Controller가 비즈니스 로직과 UI 로직을 모두 다루어, 테스트 및 유지보수가 어려움

**변경 후**

- `LottoController`는 오직 “흐름 제어”만 담당하도록 단순화
- 로또 구매 및 결과 계산 등의 핵심 비즈니스 로직은 `LottoService`로 이동
- 예외 발생 시 동일 입력 단계를 재수행하는 흐름만 유지

2. LottoResult의 역할 분리 (DTO + Calculator 구조로 개선)

**변경 전**

- `LottoResult`가 통계 계산, 상금 합산, 수익률 계산까지 모두 수행
- 데이터 보관 객체(DTO)와 계산 로직(Service)가 혼재되어 응집도가 낮았음

**변경 후**

- `LottoResult`를 **순수 데이터 전달 객체(DTO)** 로 단순화
- 통계 계산 및 수익률 로직은 **`LottoCalculator`** 클래스로 분리하여 `service` 패키지에 배치
- `LottoCalculator`는 `Lotto`와 `WinningLotto` 간의 비교를 통해 당첨 통계를 계산하고,
    
    `LottoResult`를 생성하여 Controller로 전달함


---

## 구현 흐름

```
1. `Application`에서 `LottoController` 실행  
2. `InputView`에서 구입 금액, 당첨 번호, 보너스 번호 입력  
3. `LottoMachine`이 구입 금액만큼 로또 생성  
4. `WinningLotto`와 비교하여 등수 및 수익률 계산  
5. `OutputView`에서 결과 출력
```

---

## ✅ 목표

- **작은 단위의 테스트**부터 작성하여 기능 안정성 확보해보기
- **객체 간 협력**을 통해 로또 구매 → 당첨 결과까지의 흐름을 완성하기
- **클린 코드 작성 !**
- 구현하면서 책임 분리에 관해 고민하기
- 미션 요구사항을 충실히 반영하면서, 코드의 가독성과 확장성을 높이는 연습하기
